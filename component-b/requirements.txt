# --- core pipeline ---
# Bounds are set by the exported artifacts, not by taste. See
# docs/ARCHITECTURE.md §3 for the fingerprints these must load.
numpy>=1.24
scipy>=1.11
pandas>=2.0
# The scaler is a pickled StandardScaler and the shipped one was fitted under
# 1.6.1 (docs/ARCHITECTURE.md §3). Unpickling under any other minor version
# raises InconsistentVersionWarning and is not guaranteed to be correct — it
# does not error, it just may transform differently. Re-pin when re-exporting.
scikit-learn==1.6.1
# The booster JSON is written by XGBoost 3.x.
xgboost>=3.0,<4.0
# mscgca_population.keras is the Keras 3 format. TensorFlow 2.15 ships
# Keras 2 and cannot load it — it fails at load_model, not at predict.
tensorflow>=2.16,<3.0
neurokit2>=0.2.7

# --- server ---
fastapi>=0.110
uvicorn[standard]>=0.27
websockets>=12.0
pydantic>=2.6
httpx>=0.27

# --- dev ---
pytest>=8.0
