# --- core numerics & ML ---
numpy>=1.24
pandas>=2.0
torch>=2.1
scikit-learn>=1.3

# --- audio processing ---
librosa>=0.10            # loading, resampling, spectral features
soundfile>=0.12          # wav read/write
praat-parselmouth>=0.4.3 # clinical voice measures: F0, jitter, shimmer, HNR
silero-vad>=5.1          # Layer 1 voice-activity detection (bundled weights)

# --- frozen pretrained encoder (Layer 2 embedding branch) ---
funasr>=1.1.4            # loads emotion2vec models
modelscope>=1.9          # model hub funasr downloads from

# --- API server ---
fastapi>=0.104
uvicorn>=0.24
python-multipart         # multipart audio upload support

# --- speech-to-text (companion STT stage: STT -> LLM -> TTS) ---
# Transcribes the same 16 kHz clip already uploaded for stress scoring, so the
# companion reflects on what the student actually said. CPU/int8, base model.
faster-whisper>=1.0

# --- health companion (LLM dialogue stage: STT -> LLM -> TTS) ---
# Default backend is a LOCAL Ollama model (free, private) via the stdlib -
# NO Python dependency. Install Ollama separately: https://ollama.com
# (`ollama pull qwen2.5`). anthropic is OPTIONAL - only for the Claude backend:
#   anthropic>=0.40

# --- development ---
pytest
tqdm
matplotlib
